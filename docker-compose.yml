services:
  moq:
    build:
     context: ./moq
     dockerfile: Dockerfile
    ports:
      - "8081:3000"
    restart: always
    networks:
      - net

  frontend:
    build:
     context: ./eshop
     dockerfile: Dockerfile
    ports:
      - "4200:80"
    restart: always
    networks:
      - net

networks:
  net:
    name: angular-network